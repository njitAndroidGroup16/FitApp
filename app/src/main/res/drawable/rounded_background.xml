<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="8dp" /> <!-- Adjust the radius as needed for desired rounding -->
    <solid android:color="@android:color/white" /> <!-- Background color of the item -->
    <stroke
        android:width="1dp"
        android:color="@android:color/black" /> <!-- Border color and width, optional -->
</shape>